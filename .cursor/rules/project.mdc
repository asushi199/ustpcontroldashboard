---
description: USTP Dashboard — 工作方式、技术栈与修改边界
alwaysApply: true
---

# USTP Dashboard — AI 工作方式

## 上下文与额度（优先遵守）
- 默认从最小范围入手：用户提到的路径、当前打开的文件，或先用精确搜索（符号名、文件名、路由）定位后再读相关文件。
- 不要随意做全仓库扫描（无目的地列整棵目录树、通读全部 src）。仅当任务明确要求摸清全局结构，或局部信息不足时再扩大范围。
- 改代码前只读与改动直接相关的文件；避免为顺便了解而批量打开无关模块。
- 优先用 grep/语义搜索定位 needle，再读上下文，而不是先广后窄。

## 技术栈与约定
- 前端：Vite、React 19、Tailwind CSS；以仓库根目录 package.json 为准。
- 保持与现有代码风格一致（导入、组件写法、Tailwind 类名）；不做无关重构。
- 不要提交或写入真实密钥；敏感配置用环境变量，示例用占位符。

## 修改范围
- 只改完成任务所需的文件；不擅自改 node_modules、构建产物，以及用户未要求动的本地数据文件（如 data/、*.sqlite 等）。

## 沟通
- 用户用中文时默认中文回复，除非用户要求英文。
